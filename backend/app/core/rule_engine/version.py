"""إصدار محرك القواعد.

يُختم على كل خطة مولَّدة (العمود ``plans.rule_engine_version``). بدونه، تغيير
قاعدة علمية يترك خططًا قديمة لا نعرف بأي منطق أُنتجت — وهو سؤال يُطرح فعلًا
عند مراجعة حالة تدهورت.

قواعد الترقيم:

- **major**: تغيّر قد ينتج توصية مختلفة لنفس المريض (معادلة، حد أمان، معامل)
- **minor**: قدرة جديدة لا تغيّر مخرجات الحالات القائمة
- **patch**: إصلاح لا يغيّر أي ناتج
"""

from __future__ import annotations

from typing import Final

RULE_ENGINE_VERSION: Final = "1.0.0"

__all__ = ["RULE_ENGINE_VERSION"]
